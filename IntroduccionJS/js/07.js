// Orden de las operaciones

let resultado;

resultado = (20 + 30) * 2;

resultado = (100 + 200 +300) * .2; // 20% de la suma de los 3 numeros
resultado = (600 + 600) * 1.21; // Agrega el 21% de impuestos

console.log(resultado);

let puntaje = 10;

puntaje += 10; // puntaje = puntaje + 10

console.log(puntaje); // Imprime el valor y luego lo incrementa