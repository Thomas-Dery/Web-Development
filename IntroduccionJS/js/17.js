// Diferencia entre metodos y funciones

const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); // convierte un string a un numero (funcion)
console.log(numero1.toString()); // convierte un numero a un string (metodo)

